<div class="banner banner-services" style="background-image: linear-gradient(rgba(0,0,0,0.35),rgba(0,0,0,0.35)), url(http://image.uc.cn/s/wemedia/s/upload/2022/b63b1995f7752ea2fe9ea805119329ae.jpg);">
  <div class="vcenter">
    <div class="container">
      <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-10 col-md-offset-1">
          <div class="banner-content">
            <h1 style="color:white;"><%= @bid.tender.title  %></h1>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<section class="section-bidshow;">
<div class="container" id="container container-bidshow" style="margin-top:50px;">
	<div id="recent-bidshow">
		<div class="fourcolcontent-bidshow post-bidshow">
			<h3 class="h3-bidshow" style="font-size: 24px;">Dear <%= @bid.tender.user.first_name.capitalize%> <%= @bid.tender.user.last_name.capitalize%>, </h3>
			<p style="font-size:17px;"><a href="" style="font-weight:800; text-decoration:none; color:black;"><%= @bid.user.company_name.capitalize%> Ltd </a> is pleased to present their proposal for <%= @bid.tender.title %>.
        They are a Grade A contracting company and are located at <%= @bid.user.address %></p>
		</div>
		<div class="fourcolside-bidshow" id="comments-bidshow">
			<h5 class="h5-bidshow"><a href="#"><%= cl_image_tag @bid.user.avatar.key, style:"width:75px; height:75px; border-radius:50%, object-fit:cover;"%></a></h5>
      <span style="font-style:italic; font-size:12px;"><%= (@bid.user.company_name.capitalize) ? @bid.user.company_name.capitalize : ""%> Ltd </span>
		</div>
		<div class="clear-bidshow"></div>
		</div>
	<div class="divider-bidshow"></div>
		<div id="recent-bidshow">
		<div class="fourcolcontent-bidshow post-bidshow" style="width:85%;">
			<h2 class="h2-bidshow text-center" style="font-weight:800; margin-bottom:15px; font-size:30px;"> Bid proposal</h2>
			<p style="font-size:17px;">This proposal provides an outline and estimate of costs for the <%= @bid.tender.title%> project  at <%= @bid.tender.location %>.
        Unless amended in writing, the contents of this proposal supercedes any blueprints, drawings and plans
        or verbal discussion. Materials, equipment and methods may vary from blueprints or notes provided, but
        at all times will meet or exceed building codes.</p>
      <h3 class="h3-bidshow" style="font-size: 24px;">Bid breakdown </h3>
		</div>
		<div class="clear-bidshow"></div>
    <div class="tablecontainer" style="margin-top: 20px;">
      <table class="table table-striped table-bordered ">
        <thead>
          <tr class="table-primary">
            <th scope ="col" >Name</th>
            <th scope ="col">Quantity</th>
            <th scope ="col">Unit</th>
            <th scope ="col">Unit Rate (Rs)</th>
            <th scope ="col">Amount (Rs)</th>
            <th scope ="col">Duration (Days)</th>
          </tr>
        </thead>
        <% @bid.quote  = 0 %>
        <% @bid.duration = 0 %>
        <% @bid.items.each do |item| %>
          <tr>
            <td><%= item.name %></td>
            <td><%= item.quantity %></td>
            <td><%= item.unit %></td>
            <td><%= item.unit_rate %></td>
            <td><%= number_to_currency(item.amount, unit: "") %></td>
            <td><%= item.duration %></td>
          </tr>
          <% @bid.quote  += item.amount %>
          <% @bid.duration  += item.duration %>
        <% end %>
      </table>
      <% @bid.save %>
      <p>Total Amount quoted: <%= number_to_currency(@bid.quote, unit: "Rs ") %> </p>
      <p>Works are expected to be completed in <%= @bid.duration %> days</p>
      <%= link_to "Accept Bid", activate_tender_bid_path(@bid.tender, @bid)%>
		</div>
  </div>
</div>
</section>
